{
  "name": "assr",
  "version": "1.0.0",
  "description": "Replicate the static structure and assets of any existing website by providing its URL. Perfect for learning or creating similar designs.",
  "main": "index.js",
  "scripts": {
    "start": "ts-node src/cloneWebsite.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "cheerio": "^1.0.0-rc.10"
  },
  "devDependencies": {
    "typescript": "^4.6.3",
    "ts-node": "^10.7.0"
  }
}